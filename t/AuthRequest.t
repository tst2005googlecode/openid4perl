use warnings;
use strict;
use Test::More qw( no_plan );
use English qw( -no_match_vars );
use Net::OpenID2::Message::AuthRequest;
use URI;

TODO: {
  local $TODO = 'Module is broken for now';
  ok 0;
}
